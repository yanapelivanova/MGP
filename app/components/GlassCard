// app/components/GlassCard.jsx
export default function GlassCard({ children, className = "p-6" }) {
  return (
    <div
      className={`rounded-2xl border border-white/20 bg-white/10 backdrop-blur-md shadow-lg ${className}`}
    >
      {children}
    </div>
  );
}
